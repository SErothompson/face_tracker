# face_tracker

A simple Python project for face tracking.

## Getting Started

1. Create a Python virtual environment:

```bash
python -m venv .venv
source .venv/bin/activate
```

2. Install dependencies (if any):

```bash
pip install -r requirements.txt
```

3. Run:

```bash
python main.py
```
